# Same-origin by default: on Vercel the API is served from /api on this domain.
# To point the frontend at a separately hosted backend (e.g. Railway), set
# REACT_APP_API_URL in the hosting dashboard — that takes precedence over this file.
REACT_APP_API_URL=/api
